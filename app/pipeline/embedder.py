"""
DocuMind - pipeline/embedder.py
Purpose : HuggingFace sentence-transformer embeddings (CPU only)
Phase   : 4
"""
# ============================================================
# PLACEHOLDER — implementation added in Phase 4
# ============================================================
# - get_embedder() → SentenceTransformer (lru_cache, maxsize=1)
#   device=cpu ALWAYS — no GPU on this machine
# - embed_texts(texts: list[str]) → list[list[float]]
#   batch_size=32, normalize_embeddings=True
#   returns 384-dim vectors as Python lists
# ============================================================

pass
