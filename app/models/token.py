"""
DocuMind - models/token.py
Purpose : RefreshToken ORM model for JWT refresh token rotation
Phase   : 2
"""
# ============================================================
# PLACEHOLDER — implementation added in Phase 2
# ============================================================
# Columns: id, user_id (FK), token_hash (SHA256),
#          expires_at (7 days), revoked (bool), created_at
# Note: store ONLY the hash, never the raw token
# ============================================================

pass
