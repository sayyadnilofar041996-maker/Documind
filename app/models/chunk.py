"""
DocuMind - models/chunk.py
Purpose : SQLAlchemy DocumentChunk ORM model with Vector(384) embedding
Phase   : 1
"""
# ============================================================
# PLACEHOLDER — implementation added in Phase 1
# ============================================================
# Columns: id, document_id (FK), user_id (FK),
#          text, embedding (Vector(384)), page_number,
#          chunk_index, token_count
# Note: HNSW index on embedding added in migration 003
# ============================================================

pass
