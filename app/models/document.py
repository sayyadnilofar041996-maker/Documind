"""
DocuMind - models/document.py
Purpose : SQLAlchemy Document ORM model + FileType/DocumentStatus enums
Phase   : 1
"""
# ============================================================
# PLACEHOLDER — implementation added in Phase 1
# ============================================================
# Enums: FileType (pdf|docx|py|js|ts|md)
#        DocumentStatus (pending|processing|ready|failed)
# Columns: id, user_id (FK), original_filename, stored_filename,
#          file_type, file_size_bytes, file_sha256, status,
#          chunk_count, celery_task_id, error_message,
#          created_at, updated_at
# ============================================================

pass
