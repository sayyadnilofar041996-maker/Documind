"""
DocuMind - models/session.py
Purpose : QuerySession + QueryMessage ORM models + MessageRole enum
Phase   : 1
"""
# ============================================================
# PLACEHOLDER — implementation added in Phase 1
# ============================================================
# QuerySession columns: id, user_id, document_id (nullable FK),
#                       title, created_at
# QueryMessage columns: id, session_id (FK), role (user|assistant),
#                       content, source_chunks (JSONB),
#                       latency_ms, prompt_tokens, created_at
# ============================================================

pass
