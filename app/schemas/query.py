"""
DocuMind - schemas/query.py
Purpose : Pydantic v2 schemas for RAG query, sessions, messages
Phase   : 5
"""
# ============================================================
# PLACEHOLDER — implementation added in Phase 5
# ============================================================
# - AskRequest (question, document_id?, session_id?)
# - SourceChunk (chunk_id, document_name, page_number, text, score)
# - AskResponse (answer, sources, session_id, latency_ms)
# - SessionResponse (id, title, document_id, created_at)
# - MessageResponse (id, role, content, source_chunks, latency_ms)
# ============================================================

pass
