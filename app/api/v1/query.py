"""
DocuMind - api/v1/query.py
Purpose : Query endpoints — ask (RAG), sessions CRUD
Phase   : 5
"""
# ============================================================
# PLACEHOLDER — implementation added in Phase 5
# ============================================================
# POST   /ask               → full RAG pipeline, answer + sources
# GET    /sessions          → list user sessions
# GET    /sessions/{id}     → session + full message history
# DELETE /sessions/{id}     → delete session + messages
# ============================================================

pass
