"""
DocuMind - services/auth_service.py
Purpose : Auth business logic — register, login, token rotation
Phase   : 2
"""
# ============================================================
# PLACEHOLDER — implementation added in Phase 2
# ============================================================
# - register_user(db, RegisterRequest) → User
# - authenticate_user(db, email, password) → User | None
# - issue_token_pair(db, user) → TokenResponse
# - rotate_refresh_token(db, raw_token) → TokenResponse
# - revoke_refresh_token(db, raw_token) → None
# ============================================================

pass
