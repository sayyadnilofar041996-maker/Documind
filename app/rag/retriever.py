"""
DocuMind - rag/retriever.py
Purpose : pgvector HNSW cosine similarity search for relevant chunks
Phase   : 5
"""
# ============================================================
# PLACEHOLDER — implementation added in Phase 5
# ============================================================
# - retrieve_chunks(query_embedding, user_id, doc_id=None, top_k=5)
#   → list[tuple[DocumentChunk, float]]
# - Uses pgvector <=> operator (cosine distance)
# - Filters by user_id always (user isolation)
# - Filters by similarity threshold (default 0.4)
# - Joins with documents table for filename
# ============================================================

pass
