"""
DocuMind - core/security.py
Purpose : JWT creation/decoding, password hashing, refresh token utils
Phase   : 2
"""
# ============================================================
# PLACEHOLDER — implementation added in Phase 2
# ============================================================
# - hash_password(password) → str
# - verify_password(plain, hashed) → bool
# - create_access_token(user_id) → str (15min TTL)
# - create_refresh_token() → tuple[raw, sha256_hash]
# - decode_access_token(token) → dict (raises JWTError)
# ============================================================

pass
